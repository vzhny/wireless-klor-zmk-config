manifest:
  remotes:
    - name: zmkfirmware
      url-base: https://github.com/zmkfirmware
    - name: vzhny
      url-base: https://github.com/vzhny
  projects:
    - name: zmk
      remote: zmkfirmware
      revision: v0.3.0
      import: app/west.yml
    # Pinned to a commit, not a branch -- no tag exists yet (see that
    # repo's README). Bump this deliberately, not by tracking main.
    - name: zmk-bootloader-hold
      remote: vzhny
      revision: 621fddb5621d18c99a1d1979019074191677e57d
  self:
    path: config
